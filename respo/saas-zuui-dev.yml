#公共配置
server:
  #端口
  port: 8080
zuul:
  routes:
    backstage:
      path: /backstage/**
      serviceId: saas-backstage
    certificate:
      path: /certificate/**
      serviceId: saas-certificate
